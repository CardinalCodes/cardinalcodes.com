<!-- ============ HERO ============ -->
<section class="hero" aria-labelledby="heroTitle">
  <div class="hero-bg" aria-hidden="true">
    <div class="grid"></div>
    <div class="glow"></div>
    <svg class="hero-ghost" viewBox="0 0 48 48">
      <path fill="currentColor" fill-rule="evenodd" d="M8 27 10 15 15 20 13 5 19 12 21 3 26 10 29 6 31 14 42 21 41 25 31 26 29 34 18 37 11 33Zm18.5-7a2.2 2.2 0 1 0 0 4.4 2.2 2.2 0 0 0 0-4.4Z"/>
    </svg>
  </div>

  <div class="container">
    <div>
      <p class="hero-badge" data-reveal>
        <span class="pulse-dot" aria-hidden="true"></span>
        <span data-en="Systems &amp; AI Architecture Lab" data-fr="Laboratoire d'architectures systèmes &amp; IA">Systems &amp; AI Architecture Lab</span>
      </p>

      <h1 id="heroTitle" data-reveal style="--d:.08s">
        <span data-en="Resilient" data-fr="Infrastructures">Resilient</span>
        <em data-en="infrastructure," data-fr="résilientes,">infrastructure,</em>
        <span data-en="sovereign" data-fr="pipelines IA">sovereign</span>
        <em data-en="AI pipelines" data-fr="souverains">AI pipelines</em>
        <span data-en="&amp; trusted software architectures." data-fr="&amp; architectures logicielles de confiance.">&amp; trusted software architectures.</span>
      </h1>

      <p class="lede" data-reveal style="--d:.16s" data-en="Cardinal Codes is a systems engineering lab and technical partner. We architect sovereign, high-availability cloud platforms and build specialized enterprise software designed for continuous operations." data-fr="Cardinal Codes est un laboratoire d'ingénierie système et un partenaire technique. Nous concevons des plateformes cloud souveraines à haute disponibilité et éditons des logiciels métiers conçus pour la continuité d'activité.">
        Cardinal Codes is a systems engineering lab and technical partner. We architect sovereign, high-availability cloud platforms and build specialized enterprise software designed for continuous operations.
      </p>

      <div class="hero-actions" data-reveal style="--d:.24s">
        <a class="btn btn-solid" href="#platforms">
          <span data-en="Explore Range Zero" data-fr="Découvrir Range Zero">Explore Range Zero</span>
          <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M14 5l7 7m0 0-7 7m7-7H3"/></svg>
        </a>
        <a class="btn btn-line" href="#advisory">
          <svg fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0 1 12 2.944a11.955 11.955 0 0 1-8.618 3.04A12.02 12.02 0 0 0 3 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016Z"/></svg>
          <span data-en="Services &amp; Advisory" data-fr="Nos services">Services &amp; Advisory</span>
        </a>
      </div>
    </div>

    <aside class="fieldnotes" data-reveal style="--d:.32s" aria-label="Field notes">
      <dl>
        <dt data-en="Studio" data-fr="Studio">Studio</dt><dd>Systems &amp; AI Infrastructure</dd>
        <dt data-en="Focus" data-fr="Focus">Focus</dt><dd>Sovereign Cloud · LLM · Zero-Trust</dd>
        <dt data-en="Range" data-fr="Gamme">Range</dt><dd>Range Zero — Security Suite</dd>
        <dt data-en="Status" data-fr="Statut">Status</dt><dd><span class="status"><span class="pulse-dot" aria-hidden="true"></span>OPERATIONAL</span></dd>
      </dl>
      <p class="legend" data-en="The cardinal does not migrate. It keeps watch all winter, and sings first at dawn." data-fr="Le cardinal ne migre pas. Il monte la garde tout l'hiver et chante le premier à l'aube.">
        The cardinal does not migrate. It keeps watch all winter, and sings first at dawn.
      </p>
    </aside>
  </div>
</section>

<style is:global>
/* ---------- Hero ---------- */
.hero{padding-block:clamp(4.5rem,10vw,8rem) clamp(3.5rem,8vw,6.5rem);overflow:hidden}
.hero-bg{position:absolute;inset:0;pointer-events:none}
.hero-bg .grid{
  position:absolute;inset:0;opacity:.55;
  background-image:linear-gradient(var(--line) 1px,transparent 1px),linear-gradient(90deg,var(--line) 1px,transparent 1px);
  background-size:46px 46px;
  -webkit-mask-image:radial-gradient(85% 80% at 28% 12%,#000 20%,transparent 72%);
  mask-image:radial-gradient(85% 80% at 28% 12%,#000 20%,transparent 72%);
}
.hero-bg .glow{
  position:absolute;inset:-10%;
  background:
    radial-gradient(640px 420px at 82% 6%,color-mix(in srgb,var(--carmin) 11%,transparent),transparent 66%),
    radial-gradient(520px 360px at 6% 92%,color-mix(in srgb,var(--signal) 7%,transparent),transparent 62%);
}
.hero-ghost{
  position:absolute;right:-4%;top:6%;width:clamp(300px,36vw,540px);height:auto;
  color:var(--ink);opacity:.05;transform:rotate(-5deg);
  animation:ghostFloat 9s ease-in-out infinite alternate;pointer-events:none;
}
@keyframes ghostFloat{to{transform:rotate(-3deg) translateY(-14px)}}
.hero .container{position:relative;display:grid;grid-template-columns:minmax(0,1.25fr) minmax(0,.75fr);gap:clamp(2rem,6vw,5rem);align-items:center}
.hero-badge{
  display:inline-flex;align-items:center;gap:.65rem;
  font-family:var(--font-mono);font-size:.66rem;font-weight:500;letter-spacing:.18em;text-transform:uppercase;
  color:var(--ink-2);border:1px solid var(--line);border-radius:99px;padding:.5rem 1rem;background:var(--raised);
  margin-bottom:1.6rem;
}
.hero h1{font-size:clamp(2.45rem,5.6vw,4.15rem);line-height:1.07;margin-bottom:1.4rem}
.hero .lede{font-size:clamp(1rem,1.6vw,1.13rem);color:var(--ink-2);max-width:36rem;margin-bottom:2.4rem;line-height:1.7}
.hero-actions{display:flex;flex-wrap:wrap;gap:.9rem}

/* Field notes card (hero right) */
.fieldnotes{
  position:relative;border:1px solid var(--line);border-radius:1rem;background:var(--raised);
  padding:1.5rem 1.6rem;font-family:var(--font-mono);font-size:.7rem;letter-spacing:.04em;
  box-shadow:var(--shadow);
}
.fieldnotes::before{
  content:"// FIELD NOTES — LE GUETTEUR";
  display:block;font-size:.62rem;font-weight:600;letter-spacing:.2em;color:var(--carmin-ink);
  margin-bottom:1.1rem;padding-bottom:.9rem;border-bottom:1px dashed var(--line-2);
}
.fieldnotes dl{display:grid;grid-template-columns:auto 1fr;gap:.65rem 1.4rem}
.fieldnotes dt{color:var(--ink-3);text-transform:uppercase;font-size:.62rem;letter-spacing:.14em;padding-top:.1rem}
.fieldnotes dd{color:var(--ink)}
.fieldnotes .status{display:inline-flex;align-items:center;gap:.5rem;color:var(--signal);font-weight:600}
.fieldnotes .legend{
  margin-top:1.1rem;padding-top:.9rem;border-top:1px dashed var(--line-2);
  font-family:var(--font-serif);font-style:italic;font-size:.95rem;letter-spacing:0;
  color:var(--ink-2);line-height:1.45;
}

@media (max-width:1020px){
  .hero .container{grid-template-columns:1fr}
  .fieldnotes{max-width:26rem}
}
@media (prefers-reduced-motion:reduce){
  .hero-ghost{animation:none}
}
</style>
